#!/usr/bin/python3
"""Locked class that only lets the user dynamically create the instance"""


class LockedClass:
    """Locked class that only lets the user dynamically create the instance"""
    __slots__ = ['first_name']
